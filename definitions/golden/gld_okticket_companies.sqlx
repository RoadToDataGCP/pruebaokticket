config {
  type: "incremental",
  schema: "golden_okticket",
  name: "gld_okticket_companies"
}

SELECT * FROM ${ref('stg_okticket_companies')}
WHERE fecha NOT IN (SELECT fecha FROM `golden_okticket.gld_okticket_companies`)