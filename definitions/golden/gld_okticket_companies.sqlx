config {
  type: "incremental",
  schema: "golden_okticket",
  name: "gld_okticket_companies"
}

SELECT * FROM ${ref('stg_okticket_companies')}
WHERE id NOT IN (SELECT id FROM `golden_okticket.gld_okticket_companies`)